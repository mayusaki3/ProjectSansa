package com.sansa.auth;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

class BuildSanityTest {
  @Test
  void builds() {
    // 最低限のスモーク: JUnit が起動しテストが走ることのみ確認
    assertTrue(true);
  }
}
